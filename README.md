# noon Ops Console (prototype)

A BAU + Adhoc operations console prototype for noon — regional email queue,
AI review queue, BAU checklists (Fulfillment/Logistics), knowledge base,
access control, and audit log. Frontend-only, in-memory mock data, no real backend.

## Run locally
npm install
npm run dev

## Deploy to Vercel
npx vercel deploy
# or, for a production deployment:
npx vercel deploy --prod

## Login
Username: admin123
Password: admin123
